# meta information about the serialization method
meta
{
	# required meta-information (denoted with the '.')
	.schema : "full-test-schema";
	.version : 1.0.0;
}

# definitions
def DateTime
{
	long timeSince1990;
}

def CarData
{
	DateTime timestamp;
	float ambientTemp;
}

# frame definition (a special required definition)
frame(CarData);